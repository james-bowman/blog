box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        theme: herring-cove
        config: config.toml
        flags: --disableSitemap=true
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: www.jamesbowman.me
        basedir: public
